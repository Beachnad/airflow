version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.7.5
#      - image: circleci/postgres:12.1
#        environment:
#          POSTGRES_DATABASE=airflow
    steps:
      - checkout
      - run: export PYTHONPATH=/airflow/dags/:${PYTHONPATH}
      - run: pip install -r requirements.txt
      - run: python -m unittest tests